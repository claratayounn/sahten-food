'use client'

import { useEffect, useMemo, useState } from 'react'

const logoUrl = '/sahten-logo.png'
const instagramUrl = 'https://www.instagram.com/sahtenfoodlb?igsi=MWZicTZrc29qeTBlbg=='
const supportPhone = '70490736'
type Service = { name: string; nameAr?: string; fee: number }
type Item = { name: string; en: string; price: number; category: string; prepHours: number; unit?: 'piece' | 'dozen' | 'kilo'; imageUrl?: string; services?: Service[] }
/*
  { name:'دزينة زعتر', en:'Za’atar dozen', price:6, category:'Manakish', prepHours:24 },
  { name:'دزينة لحم بعجين + صنوبر', en:'Meat & pine nuts', price:10, category:'Manakish', prepHours:24 },
  { name:'دزينة فطائر سبانخ', en:'Spinach pastries', price:10, category:'Manakish', prepHours:24 },
  { name:'دزينة شاورما لحمة', en:'Beef shawarma', price:12, category:'Manakish', prepHours:24 },
  { name:'دزينة شاورما دجاج', en:'Chicken shawarma', price:12, category:'Manakish', prepHours:24 },
  { name:'دزينة رقاقات جبنة', en:'Cheese rolls', price:9, category:'Appetizers', prepHours:24, services:[{name:'Frying',fee:3}] },
  { name:'دزينة سمبوسك + لوز', en:'Sambousek & almonds', price:10, category:'Appetizers', prepHours:24, services:[{name:'Frying',fee:3}] },
  { name:'دزينة سمبوسك + صنوبر', en:'Sambousek & pine nuts', price:12, category:'Appetizers', prepHours:24, services:[{name:'Frying',fee:3}] },
  { name:'دزينة كباب محشية لحمة + لوز', en:'Stuffed kebab with almonds', price:10, category:'Appetizers', prepHours:48, services:[{name:'Frying',fee:3}] },
  { name:'دزينة كباب محشية لحمة + صنوبر', en:'Stuffed kebab with pine nuts', price:12, category:'Appetizers', prepHours:48, services:[{name:'Frying',fee:3}] },
  { name:'دزينة جبنة', en:'Cheese manakish', price:7, category:'Manakish', prepHours:24 },
  { name:'دزينة جبنة وجبنون', en:'Cheese & mortadella', price:10, category:'Manakish', prepHours:24 },
  { name:'دزينة بيتزا', en:'Mini pizza', price:12, category:'Manakish', prepHours:24 },
  { name:'دزينة بيبروني', en:'Pepperoni', price:10, category:'Manakish', prepHours:24 },
  { name:'دزينة Hotdog ال', en:'Hotdog bites', price:10, category:'Appetizers', prepHours:24 },
  { name:'دزينة برغر', en:'Mini burgers', price:12, category:'Appetizers', prepHours:48 },
  { name:'دزينة كبيك لبنية', en:'Lebanese kibbeh bites', price:8, category:'Kibbeh', prepHours:48 },
  { name:'مخلوط extra وقية زوبع', en:'Extra mixed platter', price:6, category:'Kibbeh', prepHours:48 },
  { name:'دزينة جبنة و زيتون Boul', en:'Cheese & Boul olives', price:10, category:'Manakish', prepHours:24 },
  { name:'شيش برك ١٢٠ حبة', en:'Shish barak · 120 pieces', price:8, category:'Large Orders', prepHours:72 },
  { name:'كيلو كبة راهب', en:'Kibbeh Raheb · 1 kg', price:12, category:'Large Orders', prepHours:48 },
  { name:'كيلو محشي ملفوف', en:'Stuffed cabbage · 1 kg', price:17, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:3}] },
  { name:'كيلو ورق عنب قاطع', en:'Grape leaves · 1 kg', price:17, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:3}] },
  { name:'كيلو ورق عنب بلحم', en:'Grape leaves with meat · 1 kg', price:28, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:3}] },
  { name:'كيلو مصارين', en:'Masaareen · 1 kg', price:27, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:5}] },
  { name:'قرص كبة شحم عادي', en:'Classic kibbeh disc', price:5, category:'Kibbeh', prepHours:48, services:[{name:'Grilling',fee:3}] },
  { name:'قرص كبة بلحم و صنوبر', en:'Kibbeh with meat & pine nuts', price:7, category:'Kibbeh', prepHours:48, services:[{name:'Grilling',fee:3}] },
  { name:'قرص كبة شحم عادي لحمة و بصل', en:'Kibbeh with meat & onion', price:6, category:'Kibbeh', prepHours:48, services:[{name:'Grilling',fee:3}] },
  { name:'قرص كبة شحم صغير', en:'Small kibbeh disc', price:3.5, category:'Kibbeh', prepHours:48, services:[{name:'Grilling',fee:3}] },
  { name:'Arepa or Empanada دزينة', en:'Arepa or empanada dozen', price:15, category:'Appetizers', prepHours:48, services:[{name:'Frying',fee:3}] },
  { name:'دزينة بيتزا التونة', en:'Tuna pizza dozen', price:10, category:'Manakish', prepHours:24 },
  { name:'دزينة صفيحة طرابلسية', en:'Tripoli-style sfiha', price:25, category:'Manakish', prepHours:48 },
  { name:'دزينة سمكة حرة', en:'Spicy fish dozen', price:15, category:'Large Orders', prepHours:48 },
  { name:'كبة مسلوقة كيلو لحمة مخبوزة', en:'Boiled kibbeh · 1 kg', price:50, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:3}] },
  { name:'كبة مطبوخة بصل و سنوبر كيلو لحمة مخبوزة', en:'Cooked kibbeh with onion & pine nuts · 1 kg', price:80, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:3}] },
  { name:'كبة ممدودة بصل ولوز كيلو لحمة مخبوزة', en:'Layered kibbeh with onion & almonds · 1 kg', price:70, category:'Large Orders', prepHours:72, services:[{name:'Cooking',fee:3}] },
  { name:'كيلو كبة حمص', en:'Hummus kibbeh · 1 kg', price:13, category:'Large Orders', prepHours:48, services:[{name:'Cooking',fee:3}] },
] */
const categories = ['All','Manakish','Appetizers','Kibbeh','Large Orders']
type CartLine = { quantity:number; service?: Service }

export default function Page(){
  const [items, setItems] = useState<Item[]>([])
  const [menuLoading, setMenuLoading] = useState(true)
  const [menuError, setMenuError] = useState(false)
  const [settings, setSettings] = useState({ serviceFee: 0, deliveryFee: 3, whatsappNumber: '96100000000' })
  const [confirmation, setConfirmation] = useState<{ orderNumber: string; trackingUrl: string; whatsappLink: string } | null>(null)
  const [category,setCategory]=useState('All'), [cart,setCart]=useState<Record<string,CartLine>>({}), [showCart,setShowCart]=useState(false)
  useEffect(() => {
    fetch('/api/menu', { cache: 'no-store' })
      .then((response) => { if (!response.ok) throw new Error('menu'); return response.json() })
      .then((records) => setItems((records || []).filter((record: any) => record.available !== false).map((record: any) => ({ name: record.nameAr, en: record.nameEn, price: Number(record.price), category: record.category, prepHours: Number(record.prepHours), unit: record.unit || 'piece', imageUrl: record.imageUrl || undefined, services: (record.services || []).map((service: any) => ({ name: String(service.name), nameAr: service.nameAr ? String(service.nameAr) : undefined, fee: Number(service.fee) })) }))))
      .catch(() => setMenuError(true))
      .finally(() => setMenuLoading(false))
  }, [])
  useEffect(() => { fetch('/api/settings', { cache: 'no-store' }).then((response) => response.json()).then((value) => setSettings({ serviceFee: Number(value.serviceFee || 0), deliveryFee: Number(value.deliveryFee || 0), whatsappNumber: String(value.whatsappNumber || '96100000000') })).catch(() => undefined) }, [])
  const [fulfillment,setFulfillment]=useState<'Delivery'|'Pickup'>('Delivery'), [payment,setPayment]=useState<'Whish Money'|'OMT'>('Whish Money'), [readyDate,setReadyDate]=useState('')
  const [customer,setCustomer]=useState({name:'',phone:'',address:''}), [checkoutError,setCheckoutError]=useState(''), [checkoutSubmitting,setCheckoutSubmitting]=useState(false)
  useEffect(() => { if (checkoutError || !showCart) setCheckoutSubmitting(false) }, [checkoutError, showCart])
  const filtered=category==='All'?items:items.filter(i=>i.category===category), cartItems=items.filter(i=>cart[i.name]), count=Object.values(cart).reduce((a,b)=>a+b.quantity,0)
  const itemsSubtotal=cartItems.reduce((s,i)=>s+i.price*cart[i.name].quantity,0), servicesTotal=cartItems.reduce((s,i)=>s+(cart[i.name].service?.fee||0),0), deliveryFee=fulfillment==='Delivery'?settings.deliveryFee:0, total=itemsSubtotal+servicesTotal+settings.serviceFee+deliveryFee, deposit=Math.min(total,Math.round(total*.3*100)/100), balance=Math.max(0,Math.round((total-deposit)*100)/100)
  const maxPrep=Math.max(0,...cartItems.map(i=>i.prepHours)), today=new Date(), minDate=new Date(today.getFullYear(),today.getMonth(),today.getDate()+Math.ceil(maxPrep/24)), minDateString=`${minDate.getFullYear()}-${String(minDate.getMonth()+1).padStart(2,'0')}-${String(minDate.getDate()).padStart(2,'0')}`
  const add=(name:string)=>{setCart(c=>({...c,[name]:{quantity:(c[name]?.quantity||0)+1,service:c[name]?.service}}));setShowCart(true)}
  const remove=(name:string)=>setCart(c=>{const n={...c};if(!n[name]||n[name].quantity<=1)delete n[name];else n[name]={...n[name],quantity:n[name].quantity-1};return n})
  const setService=(name:string,service?:Service)=>setCart(c=>({...c,[name]:{...c[name],service}}))
  const validPhone=/^[0-9+\s-]{7,24}$/.test(customer.phone.trim())&&customer.phone.replace(/\D/g,'').length>=7, validDate=readyDate>=minDateString, canSend=Boolean(cartItems.length&&customer.name.trim()&&validPhone&&readyDate&&validDate&&(fulfillment==='Pickup'||customer.address.trim())), orderText=useMemo(()=>{const lines=cartItems.map(i=>{const line=cart[i.name];const service=line.service?` (${line.service.name}: +$${line.service.fee.toFixed(2)})`:'';return `• ${i.en} — ${i.name} × ${line.quantity} — $${(i.price*line.quantity).toFixed(2)}${service}`});return ['Hello Sahten Food — new booking','',`Customer: ${customer.name||'Not provided'}`,`Phone: ${customer.phone||'Not provided'}`,`Fulfillment: ${fulfillment}${fulfillment==='Delivery'?` — ${customer.address||'Address to follow'}`:' — pickup location to be sent on WhatsApp'}`,`Ready date: ${readyDate||'Not selected'}`,'','ITEMS',...lines,'',`Items subtotal: $${itemsSubtotal.toFixed(2)}`,`Preparation services: $${servicesTotal.toFixed(2)}`,`Service fee: $${settings.serviceFee.toFixed(2)}`,`${fulfillment} fee: $${deliveryFee.toFixed(2)}`,`TOTAL: $${total.toFixed(2)}`,`Deposit due now: $${deposit.toFixed(2)}`,`Balance remaining: $${balance.toFixed(2)}`,`Payment deposit method: ${payment}`].join('\n')},[customer,fulfillment,readyDate,cartItems,cart,itemsSubtotal,servicesTotal,deliveryFee,total,deposit,balance,payment])
  return <main className="creative-site">{checkoutError && <p role="alert" className="checkout-error">{checkoutError}</p>}<section className="hero-new"><header className="hero-nav wrap"><a className="logo-lockup" href="#top"><img src={logoUrl} alt="Sahten logo"/><span>SAHTEN FOOD</span></a><nav><a href="#menu">Menu</a><a href="#story">Our story</a><a href={instagramUrl} target="_blank" rel="noreferrer">Instagram ↗</a><a href="/track">Track order</a><a href="/admin">Admin</a></nav><button className="order-pill" onClick={()=>setShowCart(true)}>Your order <b>{count}</b></button></header><div className="hero-new-grid wrap"><div className="hero-statement"><p className="hero-kicker">Lebanese traditional food · Zgharta / Ehden</p><h1>Made slow.<br/><i>Shared gladly.</i></h1><p className="hero-lede">Fresh home cooking, prepared to order. Pick your favorites, choose how you want them prepared, and book your date.</p><a href="#menu" className="hero-cta">Explore the menu ↓</a></div><div className="logo-stage"><img src={logoUrl} className="hero-logo" alt="Sahten Lebanese Traditional Food"/><span className="stage-note stage-note-top">Home cooked / never rushed</span><span className="stage-note stage-note-bottom">Freshly made · made to share</span></div></div></section><section id="story" className="manifesto"><div className="wrap manifesto-grid"><p className="section-index">01 — The feeling</p><div><h2>Good food<br/><i>takes its time.</i></h2><p className="manifesto-copy">One home kitchen, small batches, serious care. Preparation time is shown for every item so you can plan the perfect gathering.</p></div><div className="manifesto-card"><strong>Plan ahead, eat better</strong><p>Some favorites need up to 72 hours. Choose a future ready date during checkout.</p></div></div></section><section id="menu" className="menu-section wrap"><div className="section-heading"><div><p className="eyebrow">02 — Choose your favorites</p><h2>The <em>home menu</em></h2></div></div><div className="filters">{categories.map(c=><button key={c} className={category===c?'active':''} onClick={()=>setCategory(c)}>{c}</button>)}</div>{menuLoading && <p className="notice">Loading the menu…</p>}{menuError && <p className="notice">We couldn&apos;t load the menu, please refresh.</p>}{!menuLoading && !menuError && items.length === 0 && <p className="notice">The menu is currently unavailable.</p>}<div className="menu-grid">{filtered.map(item=><article className="menu-card" key={item.name}>{item.imageUrl ? <img className="menu-card-image" src={item.imageUrl} alt={item.en} /> : <div className="menu-card-image menu-card-placeholder" aria-label={`${item.category} placeholder`}>{item.category}</div>}<div className="card-top"><span className="category-label">{item.category}</span><span className="price">${item.price}<small> / pack</small></span></div><h3>{item.en}</h3><p className="arabic-name" lang="ar" dir="rtl">{item.name}</p><p className="prep">Ready in {item.prepHours} hours</p>{item.services&&<p className="note">Preparation available: {item.services.map(s=>`${s.name} +$${s.fee}`).join(' · ')}</p>}<button className="add" onClick={()=>add(item.name)} aria-label={`Add ${item.en}`}>+</button></article>)}</div></section>{showCart&&<div className="overlay" onClick={()=>setShowCart(false)}><aside className="cart-drawer" role="dialog" aria-modal="true" aria-labelledby="cart-title" onClick={e=>e.stopPropagation()}><button className="close" onClick={()=>setShowCart(false)} aria-label="Close order">×</button><p className="eyebrow">Your order</p><h2 id="cart-title">Build your booking</h2>{!cartItems.length?<p className="empty">Your basket is empty.<br/>Choose something delicious from the menu.</p>:<><div className="cart-items">{cartItems.map(item=><div className="cart-row" key={item.name}><div><strong>{item.en}</strong><small lang="ar" dir="rtl">{item.name} · ${item.price}</small>{item.services&&<select className="service-select" value={cart[item.name].service?.name||''} onChange={e=>setService(item.name,item.services?.find(s=>s.name===e.target.value))}><option value="">Frozen / unprepared</option>{item.services.map(s=><option key={s.name} value={s.name}>{s.name} +${s.fee}</option>)}</select>}</div><div className="quantity"><button onClick={()=>remove(item.name)} aria-label="Decrease quantity">−</button><span>{cart[item.name].quantity}</span><button onClick={()=>add(item.name)} aria-label="Increase quantity">+</button></div></div>)}</div><label className="field-label">Your name<input className="field" value={customer.name} onChange={e=>setCustomer({...customer,name:e.target.value})} required/></label><label className="field-label">Phone number<input className="field" value={customer.phone} onChange={e=>setCustomer({...customer,phone:e.target.value})} required/></label><label className="field-label">Ready date (Lebanon time)<input className="field" type="date" min={minDateString} value={readyDate} onChange={e=>setReadyDate(e.target.value)} required/></label><small className="field-help">Earliest date based on the slowest item: {maxPrep||24} hours.</small><label className="field-label">Fulfillment</label><div className="choice-row">{['Delivery','Pickup'].map(c=><button key={c} className={fulfillment===c?'selected':''} onClick={()=>setFulfillment(c as 'Delivery'|'Pickup')}>{c}</button>)}</div>{fulfillment==='Delivery'?<label className="field-label">Delivery address<input className="field" value={customer.address} onChange={e=>setCustomer({...customer,address:e.target.value})} required/></label>:<p className="policy">Pickup location will be sent by WhatsApp after booking confirmation.</p>}<label className="field-label">Deposit method</label><div className="choice-row">{['Whish Money','OMT'].map(p=><button key={p} className={payment===p?'selected':''} onClick={()=>setPayment(p as 'Whish Money'|'OMT')}>{p}</button>)}</div><div className="breakdown"><p><span>Items subtotal</span><b>${itemsSubtotal.toFixed(2)}</b></p><p><span>Preparation services</span><b>${servicesTotal.toFixed(2)}</b></p><p><span>{fulfillment} fee</span><b>${deliveryFee.toFixed(2)}</b></p><p className="grand"><span>Total</span><b>${total.toFixed(2)}</b></p><p><span>Deposit due now</span><b>${deposit.toFixed(2)}</b></p><p><span>Balance remaining</span><b>${balance.toFixed(2)}</b></p></div><button className="button primary full" onClick={async()=>{if(checkoutSubmitting)return;setCheckoutSubmitting(true);setCheckoutError('');if(!readyDate||!customer.name||!customer.phone||(fulfillment==='Delivery'&&!customer.address)){setCheckoutError('Please complete your name, phone, address, and ready date.');return}const result=await fetch('/api/orders',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({customerName:customer.name,customerPhone:customer.phone,address:customer.address,fulfillment:fulfillment.toLowerCase(),readyDate,items:cartItems.map(i=>({...i,quantity:cart[i.name].quantity,service:cart[i.name].service})),itemsSubtotal,servicesTotal,deliveryFee,total,depositDue:deposit,balanceRemaining:balance,paymentMethod:payment==='Whish Money'?'whish':'omt'})});const saved=await result.json();if(!result.ok){setCheckoutError('We could not save this booking. Please try again.');return}window.open(`https://wa.me/961${customer.phone.trim().replace(/^0+/, '')}?text=${encodeURIComponent(`${orderText}\nOrder number: ${saved.orderNumber}`)}`,'_blank','noopener,noreferrer')}}>{"Send booking to WhatsApp ↗"}</button></>}</aside></div>}<section className="service-strip"><div className="wrap service-grid"><div><p className="eyebrow">03 — Made for your day</p><h2>Choose how<br/><em>you’ll receive it.</em></h2></div><div className="service-copy"><div><strong>Delivery</strong><p>Tell us where to bring your order, or finalize the details on WhatsApp.</p></div><div><strong>Pickup</strong><p>We will send you the exact pickup location via WhatsApp once your order is confirmed.</p></div></div></div></section><section className="contact-new"><div className="wrap"><p className="eyebrow">04 — Make it a gathering</p><div className="contact-new-content"><h2>Bring people.<br/><em>We’ll bring the food.</em></h2><a className="contact-button" href="#menu">Plan your order ↗</a></div><footer className="footer"><a className="logo-lockup" href="#top"><img src={logoUrl} alt="Sahten logo"/><span>SAHTEN FOOD</span></a><span>Delivering across Zgharta in winter &amp; Ehden in summer.</span><a href={instagramUrl} target="_blank" rel="noreferrer">@sahtenfoodlb ↗</a></footer></div></section></main>
}
